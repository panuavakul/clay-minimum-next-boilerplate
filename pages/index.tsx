import { Typography } from '@mui/material';
import { Container } from '@mui/system';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <Container>
      <Typography>Hello Minimum Next with MUI and Emotion</Typography>
    </Container>
  );
};

export default Home;
